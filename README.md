# agentmine

**AI Project Manager** - Redmine for AI Agents

> AIエージェントと人間が協調してプロジェクト管理を行うためのツール

## コンセプト

### 背景・課題

現代のソフトウェア開発では、AIエージェント（Claude Code, Codex CLI, Gemini CLI等）が実装を担当することが増えています。しかし、既存のプロジェクト管理ツール（GitHub Issues, Jira, Redmine等）は人間向けに設計されており、AIエージェントとの協調作業には最適化されていません。

**現状の課題:**
- AIエージェントからの操作が冗長（`gh issue list/create/close`）
- 人間向けUIなのでAIにとって情報密度が低い
- タスクの担当（AI/人間）の区別がない
- AIエージェントの実行履歴・コンテキストが残らない

### 解決策

agentmineは「AI版Redmine」として、以下を提供します：

1. **CLI**: AIエージェント向けの高速・構造化インターフェース
2. **Web UI**: 人間向けのビジュアルダッシュボード
3. **エージェント定義**: 役割別AIエージェントの設定管理
4. **スキルシステム**: 再利用可能なタスクテンプレート

## アーキテクチャ

```
┌──────────────────────────────────────────────────────────────┐
│                        agentmine                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────┐          ┌─────────────┐                  │
│   │     CLI     │          │   Web UI    │                  │
│   │ (TypeScript)│          │  (Next.js)  │                  │
│   │             │          │             │                  │
│   │  AI向け     │          │  人間向け    │                  │
│   └──────┬──────┘          └──────┬──────┘                  │
│          │                        │                          │
│          └────────┬───────────────┘                          │
│                   ▼                                          │
│          ┌───────────────┐                                   │
│          │   Database    │                                   │
│          │ SQLite / PG   │                                   │
│          └───────────────┘                                   │
│                   │                                          │
│   ┌───────────────┼───────────────┐                          │
│   ▼               ▼               ▼                          │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐                         │
│ │Projects │ │ Agents  │ │ Skills  │                         │
│ │ Tasks   │ │ Sessions│ │ Prompts │                         │
│ │History  │ │         │ │         │                         │
│ └─────────┘ └─────────┘ └─────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

## 主要概念

### 1. タスク (Tasks)

Redmineの「チケット」に相当。開発作業の単位。

```yaml
Task:
  id: 1
  title: "ユーザー認証機能の実装"
  status: open | in_progress | review | done | cancelled
  priority: low | medium | high | critical
  type: task | feature | bug | refactor
  assignee_type: ai | human
  assignee_name: "coder"  # エージェント名 or 人名
  branch_name: "task-1-auth"
  pr_url: "https://github.com/..."
```

**ステータスフロー:**
```
open → in_progress → review → done
         ↓            ↓
      cancelled   cancelled
```

### 2. エージェント (Agents)

役割別のAI設定。モデル・ツール・スキルを定義。

```yaml
agents:
  coder:
    description: "コード実装担当"
    model: claude-sonnet
    tools: [Read, Write, Edit, Bash, Grep, Glob]
    skills: [commit, test, debug]

  reviewer:
    description: "コードレビュー担当"
    model: claude-haiku
    tools: [Read, Grep, Glob]
    skills: [review]

  architect:
    description: "設計・計画担当"
    model: claude-opus
    tools: [Read, Grep, Glob, WebSearch]
    skills: [plan, design]
```

### 3. スキル (Skills)

再利用可能なプロンプトテンプレート。

```yaml
skills:
  # 組み込みスキル
  commit:
    source: builtin

  # ローカルスキル（カスタム）
  deploy:
    source: local
    path: .agentmine/skills/deploy.md

  # リモートスキル
  security-review:
    source: remote
    url: https://example.com/skills/security.md
```

### 4. セッション (Sessions)

エージェントの実行履歴。コンテキスト保存。

```yaml
Session:
  id: 1
  task_id: 3
  agent_name: "coder"
  status: running | completed | failed
  input: "認証機能を実装してください"
  output: "実装完了。JWT認証を追加しました..."
  token_usage: 15000
```

## CLI使用例

### プロジェクト初期化
```bash
agentmine init
# → .agentmine/config.yaml 生成
```

### タスク管理
```bash
# タスク作成
agentmine task add "認証機能実装" -p high -t feature

# 一覧表示
agentmine task list
# ID  Status       Priority  Type     Assignee  Title
# #1  open         high      feature  -         認証機能実装

# AIエージェントに割り当て
agentmine task assign 1 coder --ai

# 作業開始（ブランチ自動作成）
agentmine task start 1
# → git checkout -b task-1-auth

# 完了（PR自動作成）
agentmine task done 1
# → git push && gh pr create
```

### エージェント操作
```bash
# エージェント一覧
agentmine agent list

# エージェント実行
agentmine agent run coder "タスク1の認証機能を実装してください"

# 特定タスクに紐づけて実行
agentmine agent run coder --task 1 "実装を進めてください"
```

### スキル実行
```bash
# スキル一覧
agentmine skill list

# スキル即時実行
agentmine skill run commit
agentmine skill run test
agentmine skill run review
```

### Web UI
```bash
# ダッシュボード起動
agentmine ui
# → http://localhost:3333 で開く
```

## Web UI機能

```
┌─────────────────────────────────────────────────────────────┐
│  📋 Dashboard                                    [user] ▼  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │  Open    │ │ In Prog  │ │  Review  │ │   Done   │      │
│  │   (5)    │ │   (2)    │ │   (1)    │ │   (12)   │      │
│  ├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤      │
│  │ #7 認証  │ │ #5 API   │ │ #3 UI    │ │ #1 ...   │      │
│  │ 🤖 coder │ │ 🤖 coder │ │ 👤 human │ │          │      │
│  │          │ │          │ │          │ │          │      │
│  │ #8 ...   │ │ #6 ...   │ │          │ │ #2 ...   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│                                                             │
│  Recent Activity                                            │
│  ─────────────────────────────────────────────────────────  │
│  🤖 coder completed #5 API実装              2 min ago      │
│  👤 You assigned #7 to coder                5 min ago      │
│  🤖 coder started #6                        10 min ago     │
└─────────────────────────────────────────────────────────────┘
```

**機能:**
- カンバンボード（ドラッグ&ドロップ）
- タイムライン表示
- エージェント設定GUI
- セッション履歴閲覧

## プロジェクト設定

`.agentmine/config.yaml`:

```yaml
project:
  name: MyProject
  description: "プロジェクトの説明"

# データベース設定
database:
  # ローカル開発
  url: file:./data.db
  # または チーム共有（PostgreSQL）
  # url: postgres://user:pass@host:5432/agentmine

# エージェント定義
agents:
  coder:
    description: "コード実装担当"
    model: claude-sonnet
    tools:
      - Read
      - Write
      - Edit
      - Bash
      - Grep
      - Glob
    skills:
      - commit
      - test
      - debug

  reviewer:
    description: "コードレビュー担当"
    model: claude-haiku
    tools:
      - Read
      - Grep
      - Glob
    skills:
      - review

# スキル定義
skills:
  deploy:
    source: local
    path: .agentmine/skills/deploy.md

# Git連携
git:
  branch_prefix: "task-"
  auto_pr: true
  pr_template: |
    ## Summary
    {{task.title}}

    ## Changes
    {{changes}}
```

## 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| CLI | TypeScript (Node.js) | Web UIと言語統一、型共有 |
| Web UI | Next.js + shadcn/ui | モダンUI、高速開発 |
| DB | SQLite / PostgreSQL | ローカル↔チーム両対応 |
| ORM | Drizzle | 型安全、軽量、両DB対応 |
| Monorepo | pnpm workspace + Turborepo | 効率的な開発 |

```
agentmine/
├── packages/
│   ├── cli/           # CLIアプリケーション
│   ├── web/           # Next.js Web UI
│   └── core/          # 共有ロジック（DB、型定義）
├── pnpm-workspace.yaml
├── turbo.json
└── package.json
```

## ロードマップ

### Phase 1: MVP（現在）
- [x] プロジェクト構造セットアップ
- [ ] CLI基本コマンド実装
- [ ] SQLiteデータベース統合
- [ ] タスクCRUD操作

### Phase 2: エージェント統合
- [ ] Claude Code連携
- [ ] エージェント実行機能
- [ ] セッション記録

### Phase 3: Web UI
- [ ] Next.jsダッシュボード
- [ ] カンバンボード
- [ ] リアルタイム更新

### Phase 4: チーム機能
- [ ] PostgreSQL対応
- [ ] 複数ユーザー管理
- [ ] 権限管理

### Phase 5: 拡張
- [ ] GitHub Issues同期
- [ ] Slack/Discord通知
- [ ] メトリクスダッシュボード

## 開発

```bash
# 依存関係インストール
pnpm install

# CLI開発
pnpm cli dev

# Web UI開発
pnpm web dev

# ビルド
pnpm build

# CLIをグローバルインストール
cd packages/cli && npm link
```

## 関連プロジェクト

- [Claude Code](https://claude.ai/claude-code) - Anthropic公式CLI
- [Codex CLI](https://github.com/openai/codex-cli) - OpenAI Codex CLI
- [Gemini CLI](https://github.com/google/gemini-cli) - Google Gemini CLI
- [CrewAI](https://www.crewai.com/) - AIエージェントフレームワーク
- [DevHive](https://github.com/...) - Git Worktree + 並列AI開発

## License

MIT
